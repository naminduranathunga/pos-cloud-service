

/**
 * Check weather the current action is permitted for the current company based on their package.
 * It checks for package limits, trial period, etc.
 * @param action 
 * @param company 
 * @returns boolean
 */
export function PkgCheckActionPermitted(action:string, company:any){
    return true;
}